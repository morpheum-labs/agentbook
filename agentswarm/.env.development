# Do not set VITE_API_URL for local dev: the Vite server (vite.config server.proxy) sends /api to
# clawgotcha at 127.0.0.1:3477. apiOrigin() is "" so fetch uses same-origin /api/... and hits the proxy.
# Production API origin is in .env.production (used by `bun run build`).