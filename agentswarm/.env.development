# Do not set VITE_API_URL for local dev: the Vite server proxies /api to clawlaundry at 127.0.0.1:3458 (align with dep/cl.yaml; see vite.config.ts). A wrong port causes NetworkError or ECONNREFUSED in the proxy.
# For production or preview, set VITE_API_URL to the public API origin at build time.
